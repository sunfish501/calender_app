// auth-ui.js — placeholder for future login/signup UI.
// Today the calendar runs without authentication; this file exists so the
// loading order matches the planned structure. When auth is enabled, render
// a login button/modal here and wire it to window.SupabaseAuth.
(function () {
  if (!window.SupabaseAuth || !window.SupabaseAuth.enabled) return;
  // TODO: build login/signout UI and mount it next to the existing header.
})();
